@model IServiceManager


<div class="table-agile-info">
    <div class="panel panel-default">
        @*title*@
        <div class="panel-heading"></div>
        @*/title*@
        @*table menubar*@
        <div class="row w3-res-tb">
            @*options*@
            <div class="col-sm-5 m-b-xs">
				<select id="slct_tableMenubar" class="input-sm form-control w-sm inline v-middle">
                </select>
				<button id="btn_apply" class="btn btn-sm btn-default">
                </button>                
            </div>
            @*/options*@
            <div class="col-sm-4"></div>
			@*search bar*@
            @*<div class="col-sm-3">
				<div class="input-group">
					<input type="text" class="input-sm form-control" placeholder="Ara">
					<span class="input-group-btn">
						<button class="btn btn-sm btn-default" type="button">
							Ara
						</button>
					</span>
				</div>
            </div>*@
			@*/search bar*@
        </div>
        @*/table menubar*@

        @*/data*@
        <div class="table-responsive">
            <table id="tbl_user" class="table table-striped b-t b-light">
                <thead>
                    <tr>
                        <th style="width:20px;">
                            <label class="i-checks m-b-none">
								<input id="box_all" type="checkbox"><i></i>
                            </label>
                        </th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </div>
        @*/data*@

        @*/navigation*@
        <footer class="panel-footer">
            <div class="row">
                <div class="col-sm-5 text-left">
                    <small id="lbl_entityQuantity" class="text-muted inline m-t-sm m-b-s"></small>
                </div>
                <div class="col-sm-7 text-right text-center-xs">                
                    <ul id="ul_pagination" class="pagination pagination-sm m-t-none m-b-none"></ul>
                </div>
            </div>
        </footer>
        @*/navigation*@
    </div>
</div>

<script type="module" src="~/js/miar_user_display.js"></script>

<script>
    const tableTitleByLanguages = {
        "TR": "KAYITLI KULLANICILAR",
        "EN": "REGISTERED USERS"
    };
    const tableMenubar_optionsByLanguages = {
        "TR": [
            "Seçilenleri Sil"
        ],
        "EN": [
            "Delete Selected"
        ]
    }
    const tableMenubar_applyButtonName = {
        "TR": "Uygula",
        "EN": "Apply"
    };
    const columnNamesByLanguages = {
        "TR": {
            "firstName": "Ad",
            "lastName": "Soyad",
            "companyName": "Şirket",
            "telNo": "Telefon",
            "email": "Email",
            "roleNames": "Rol",
            "createdAt": "Kayıt Tarihi",
            "processes": "İşlemler",
        },
        "EN": {
            "firstName": "First Name",
            "lastName": "Last Name",
            "companyName": "Company",
            "telNo": "Telephone",
            "email": "Email",
            "roleNames": "Role",
            "createdAt": "Registration Date",
            "processes": "Processes",
        }
    }
    const columnNamesToBeFill = [
        "firstName",
        "lastName",
        "companyName",
        "telNo",
        "email",
        "roleNames",
        "createdAt"
    ]
    const entityQuantityMessageByLanguages = {
        "TR": "kullanıcı gösteriliyor",
        "EN": "user displaying"
    };
    const updateButtonNameByLanguages = {
        "TR": "Güncelle",
        "EN": "Update",
    };
</script>
